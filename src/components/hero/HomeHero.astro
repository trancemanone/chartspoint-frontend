---
/**
 * HomeHero Component - Calm, Analytical Hero Section
 *
 * ChartsPoint homepage hero section featuring:
 * - Calm, analytical messaging (NO hype, NO promises)
 * - Educational focus per Vision Document
 * - 4-pillar quick links
 * - Abstract chart illustration
 *
 * Per Vision Document:
 * - النبرة: هادئة، تحليلية، بلا وعود
 * - "تعليم التحليل الفني بعيدا عن الضجيج"
 * - "فهم الشارت قبل مطاردة الإشارات"
 * - "التحليل الفني كمنهج لا كحظ"
 *
 * Optimized for Arabic readers ages 40-70 with WCAG AAA compliance.
 */

import { PILLARS } from '../../lib/types';

// Educational CTAs per Vision - FORBIDDEN: اشترك الآن, ابدأ التداول, أرباح, فرص
const primaryCTA = {
  label: 'ابدأ من الأساسيات',
  href: '/basics',
  ariaLabel: 'ابدأ رحلة التعلم من أساسيات التحليل الفني',
};

const secondaryCTA = {
  label: 'استكشف المحتوى',
  href: '#pillars',
  ariaLabel: 'استكشف أقسام المحتوى التعليمي',
};

// Pillar quick links
const pillarLinks = [
  { ...PILLARS.basics, icon: 'book', href: '/basics' },
  { ...PILLARS.indicators, icon: 'chart', href: '/indicators' },
  { ...PILLARS.tools, icon: 'calculator', href: '/tools' },
  { ...PILLARS.tactics, icon: 'shield', href: '/tactics' },
];
---

<section
  class="hero-section relative overflow-hidden"
  aria-labelledby="hero-heading"
>
  <!-- Subtle background pattern -->
  <div class="hero-bg absolute inset-0 -z-10"></div>

  <div class="container mx-auto px-4 lg:px-6">
    <div class="hero-grid grid lg:grid-cols-12 gap-8 lg:gap-12 items-center min-h-[calc(100vh-88px)] lg:min-h-[600px] py-12 lg:py-20">

      <!-- Content Column -->
      <div class="lg:col-span-7 xl:col-span-6">
        <!-- Badge -->
        <div class="hero-badge inline-flex items-center gap-2 px-4 py-2 rounded-full mb-6">
          <span class="badge-dot w-2 h-2 rounded-full bg-gold-500 animate-pulse"></span>
          <span class="text-sm font-medium text-gold-700">التحليل الفني للأسواق المالية</span>
        </div>

        <!-- Heading - Calm, Analytical -->
        <h1
          id="hero-heading"
          class="hero-title font-heading text-h1-mobile md:text-h1-desktop text-navy-900 mb-6"
        >
          تعليم التحليل الفني
          <span class="text-gold-600">بعيدا عن الضجيج</span>
        </h1>

        <!-- Subtitle - Educational, Analytical Tone -->
        <p class="hero-subtitle text-body-lg text-slate-600 leading-relaxed mb-8 max-w-xl">
          نحو فهم أعمق لكيف تتحرك الأسعار، ولماذا تفشل الإشارات، وكيف يدير المتداول المخاطر قبل التفكير في الأرباح.
        </p>

        <!-- CTA Buttons - Educational Focus -->
        <div class="hero-ctas flex flex-wrap items-center gap-4 mb-12">
          <a
            href={primaryCTA.href}
            class="cta-primary inline-flex items-center gap-2 px-6 py-3.5 rounded-lg font-semibold text-base transition-all duration-200"
            aria-label={primaryCTA.ariaLabel}
          >
            <span>{primaryCTA.label}</span>
            <svg class="w-5 h-5 rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>

          <a
            href={secondaryCTA.href}
            class="cta-secondary inline-flex items-center gap-2 px-6 py-3.5 rounded-lg font-medium text-base transition-all duration-200"
            aria-label={secondaryCTA.ariaLabel}
          >
            <span>{secondaryCTA.label}</span>
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </a>
        </div>

        <!-- Trust Indicators - Calm, Factual -->
        <div class="trust-indicators flex flex-wrap items-center gap-6 text-sm text-slate-500">
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-tools-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.75" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
            </svg>
            <span>محتوى تعليمي موثق</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-tactics-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.75" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
            <span>بدون توصيات</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-indicators-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.75" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
            <span>التركيز على إدارة المخاطر</span>
          </div>
        </div>
      </div>

      <!-- Visual Column - Abstract Chart Illustration -->
      <div class="hidden lg:block lg:col-span-5 xl:col-span-6">
        <div class="hero-visual relative">
          <!-- Abstract chart illustration -->
          <div class="chart-illustration">
            <svg
              viewBox="0 0 400 300"
              fill="none"
              class="w-full h-auto"
              aria-hidden="true"
            >
              <!-- Grid lines -->
              <defs>
                <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
                  <path d="M 40 0 L 0 0 0 40" fill="none" stroke="rgba(226,232,240,0.5)" stroke-width="1"/>
                </pattern>
              </defs>
              <rect width="400" height="300" fill="url(#grid)" />

              <!-- Candlestick shapes -->
              <g class="candlesticks">
                <!-- Green candle 1 -->
                <rect x="50" y="120" width="16" height="80" rx="2" fill="#10B981" opacity="0.8"/>
                <line x1="58" y1="100" x2="58" y2="120" stroke="#10B981" stroke-width="2"/>
                <line x1="58" y1="200" x2="58" y2="220" stroke="#10B981" stroke-width="2"/>

                <!-- Red candle 2 -->
                <rect x="90" y="100" width="16" height="60" rx="2" fill="#EF4444" opacity="0.7"/>
                <line x1="98" y1="80" x2="98" y2="100" stroke="#EF4444" stroke-width="2"/>
                <line x1="98" y1="160" x2="98" y2="180" stroke="#EF4444" stroke-width="2"/>

                <!-- Green candle 3 -->
                <rect x="130" y="90" width="16" height="70" rx="2" fill="#10B981" opacity="0.9"/>
                <line x1="138" y1="70" x2="138" y2="90" stroke="#10B981" stroke-width="2"/>
                <line x1="138" y1="160" x2="138" y2="175" stroke="#10B981" stroke-width="2"/>

                <!-- Green candle 4 -->
                <rect x="170" y="70" width="16" height="90" rx="2" fill="#10B981" opacity="0.85"/>
                <line x1="178" y1="50" x2="178" y2="70" stroke="#10B981" stroke-width="2"/>
                <line x1="178" y1="160" x2="178" y2="180" stroke="#10B981" stroke-width="2"/>

                <!-- Red candle 5 -->
                <rect x="210" y="80" width="16" height="50" rx="2" fill="#EF4444" opacity="0.7"/>
                <line x1="218" y1="60" x2="218" y2="80" stroke="#EF4444" stroke-width="2"/>
                <line x1="218" y1="130" x2="218" y2="150" stroke="#EF4444" stroke-width="2"/>

                <!-- Green candle 6 -->
                <rect x="250" y="60" width="16" height="80" rx="2" fill="#10B981" opacity="0.9"/>
                <line x1="258" y1="40" x2="258" y2="60" stroke="#10B981" stroke-width="2"/>
                <line x1="258" y1="140" x2="258" y2="160" stroke="#10B981" stroke-width="2"/>

                <!-- Green candle 7 -->
                <rect x="290" y="50" width="16" height="60" rx="2" fill="#10B981" opacity="0.95"/>
                <line x1="298" y1="35" x2="298" y2="50" stroke="#10B981" stroke-width="2"/>
                <line x1="298" y1="110" x2="298" y2="130" stroke="#10B981" stroke-width="2"/>

                <!-- Red candle 8 -->
                <rect x="330" y="60" width="16" height="40" rx="2" fill="#EF4444" opacity="0.65"/>
                <line x1="338" y1="45" x2="338" y2="60" stroke="#EF4444" stroke-width="2"/>
                <line x1="338" y1="100" x2="338" y2="115" stroke="#EF4444" stroke-width="2"/>
              </g>

              <!-- Trend line -->
              <path
                d="M40 200 Q100 160 160 140 T280 80 T360 60"
                stroke="#d5a035"
                stroke-width="3"
                fill="none"
                stroke-linecap="round"
                opacity="0.8"
              />

              <!-- Moving average line -->
              <path
                d="M40 180 Q120 150 200 130 T360 90"
                stroke="#3B82F6"
                stroke-width="2"
                fill="none"
                stroke-linecap="round"
                stroke-dasharray="8 4"
                opacity="0.6"
              />

              <!-- Volume bars at bottom -->
              <g class="volume" opacity="0.4">
                <rect x="50" y="260" width="16" height="30" fill="#10B981"/>
                <rect x="90" y="270" width="16" height="20" fill="#EF4444"/>
                <rect x="130" y="255" width="16" height="35" fill="#10B981"/>
                <rect x="170" y="250" width="16" height="40" fill="#10B981"/>
                <rect x="210" y="265" width="16" height="25" fill="#EF4444"/>
                <rect x="250" y="245" width="16" height="45" fill="#10B981"/>
                <rect x="290" y="255" width="16" height="35" fill="#10B981"/>
                <rect x="330" y="270" width="16" height="20" fill="#EF4444"/>
              </g>
            </svg>

            <!-- Floating indicator badges -->
            <div class="indicator-badge indicator-badge-1">
              <span class="text-xs font-semibold text-white">RSI: 65</span>
            </div>
            <div class="indicator-badge indicator-badge-2">
              <span class="text-xs font-semibold text-white">MACD +</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* ═══════════════════════════════════════════════════════════════════════════
     HERO SECTION BASE
     ═══════════════════════════════════════════════════════════════════════════ */

  .hero-section {
    background: linear-gradient(180deg, #F6F8FA 0%, #FFFFFF 50%, #F8FAFC 100%);
  }

  .hero-bg {
    background:
      radial-gradient(at 0% 0%, rgba(213, 160, 53, 0.05) 0px, transparent 50%),
      radial-gradient(at 100% 0%, rgba(59, 130, 246, 0.04) 0px, transparent 50%),
      radial-gradient(at 100% 100%, rgba(139, 92, 246, 0.03) 0px, transparent 50%);
  }

  /* ═══════════════════════════════════════════════════════════════════════════
     BADGE STYLES
     ═══════════════════════════════════════════════════════════════════════════ */

  .hero-badge {
    background: rgba(253, 248, 237, 0.9);
    border: 1px solid rgba(213, 160, 53, 0.2);
  }

  /* ═══════════════════════════════════════════════════════════════════════════
     TITLE STYLES - Generous line-height for Arabic
     ═══════════════════════════════════════════════════════════════════════════ */

  .hero-title {
    line-height: 1.35;
    letter-spacing: -0.01em;
  }

  @media (min-width: 768px) {
    .hero-title {
      line-height: 1.25;
    }
  }

  /* ═══════════════════════════════════════════════════════════════════════════
     CTA BUTTON STYLES
     ═══════════════════════════════════════════════════════════════════════════ */

  .cta-primary {
    background: linear-gradient(135deg, #e8bc5a 0%, #d5a035 100%);
    color: theme('colors.navy.900');
    box-shadow: 0 4px 14px rgba(213, 160, 53, 0.25);
    border: 1px solid rgba(213, 160, 53, 0.3);
  }

  .cta-primary:hover {
    background: linear-gradient(135deg, #d5a035 0%, #b8892a 100%);
    box-shadow: 0 6px 20px rgba(213, 160, 53, 0.4);
    transform: translateY(-1px);
  }

  .cta-primary:focus-visible {
    outline: none;
    box-shadow: 0 0 0 3px white, 0 0 0 6px theme('colors.gold.500');
  }

  .cta-secondary {
    background: white;
    color: theme('colors.navy.700');
    border: 1px solid theme('colors.slate.200');
    box-shadow: 0 2px 4px rgba(15, 23, 42, 0.04);
  }

  .cta-secondary:hover {
    background: theme('colors.slate.50');
    border-color: theme('colors.slate.300');
    transform: translateY(-1px);
  }

  .cta-secondary:focus-visible {
    outline: none;
    box-shadow: 0 0 0 3px white, 0 0 0 6px theme('colors.gold.500');
  }

  /* ═══════════════════════════════════════════════════════════════════════════
     CHART ILLUSTRATION
     ═══════════════════════════════════════════════════════════════════════════ */

  .chart-illustration {
    position: relative;
    background: white;
    border-radius: 16px;
    border: 1px solid theme('colors.slate.200');
    box-shadow: 0 8px 32px rgba(15, 23, 42, 0.08);
    padding: 24px;
    overflow: hidden;
  }

  .indicator-badge {
    position: absolute;
    padding: 6px 12px;
    border-radius: 6px;
    box-shadow: 0 4px 12px rgba(15, 23, 42, 0.15);
  }

  .indicator-badge-1 {
    top: 20%;
    right: 15%;
    background: linear-gradient(135deg, #10B981 0%, #059669 100%);
  }

  .indicator-badge-2 {
    bottom: 30%;
    left: 10%;
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
  }

  /* ═══════════════════════════════════════════════════════════════════════════
     REDUCED MOTION
     ═══════════════════════════════════════════════════════════════════════════ */

  @media (prefers-reduced-motion: reduce) {
    .cta-primary,
    .cta-secondary,
    .badge-dot {
      transition: none;
      animation: none;
    }
  }
</style>
