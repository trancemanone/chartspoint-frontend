---
/**
 * Tools Pillar Index Page
 *
 * Practical Utilities pillar for ChartsPoint
 * Clusters: calc, charts, data
 * Color: Green #10B981
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import { PILLARS, CLUSTERS } from '@lib/types';

const pillar = PILLARS.tools;
const clusters = CLUSTERS.filter(c => c.pillar === 'tools');

const pageTitle = pillar.nameAr;
const pageDescription = pillar.description;
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  pillar="tools"
>
  <section class="min-h-screen py-16 px-4">
    <div class="container mx-auto max-w-6xl">
      <!-- Pillar Header -->
      <header class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold text-navy-900 mb-4">
          {pillar.nameAr}
        </h1>
        <p class="text-xl text-slate-600 max-w-2xl mx-auto">
          {pillar.description}
        </p>
        <div class="mt-4 w-24 h-1 mx-auto rounded-full" style={`background-color: ${pillar.color}`}></div>
      </header>

      <!-- Clusters Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {clusters.map(cluster => (
          <a
            href={`/tools/${cluster.slug}/`}
            class="block p-6 bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow border-r-4"
            style={`border-color: ${pillar.color}`}
          >
            <h2 class="text-xl font-semibold text-navy-900 mb-2">
              {cluster.nameAr}
            </h2>
            <p class="text-slate-600">
              استكشف {cluster.nameAr}
            </p>
          </a>
        ))}
      </div>

      <!-- Placeholder Notice -->
      <div class="mt-12 p-6 bg-green-50 rounded-xl text-center">
        <p class="text-green-800">
          هذه صفحة مؤقتة. سيتم إضافة المحتوى قريباً.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
